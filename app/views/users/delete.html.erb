
<div class="round">
  <%= link_to 'Back', {:action => 'index'} %><br />
  <h2>Delete User</h2>
  <div class="container bg-info">
<%= form_tag({:action => 'destroy', :id => @user.username}) do %>
    <p>Are you sure you want to permanently delete this user?</p>
    <p><%= @user.username %></p>
    <%= submit_tag("Delete User") %>
<% end %>
  </div>
</div>